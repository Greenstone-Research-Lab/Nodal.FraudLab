@page "/graph-visualization"

<PageTitle>Graph visualization</PageTitle>

<section class="page-heading"><p class="eyebrow">EVIDENCE EXPLORER</p><h1>See the reason behind a risk score.</h1><p>The future canvas will render case-scoped subgraphs: customer, account, shared device, address, transaction, merchant, and rule evidence.</p></section>
<section class="next-step"><h2>Provider-neutral output</h2><p>The graph adapter will return a normalised node-and-edge payload, so the UI will not depend on Neo4j, TigerGraph, or a later provider.</p></section>
